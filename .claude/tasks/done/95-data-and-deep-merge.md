## Problem to solve

`fetch` and `patch` do not only apply to Tuples, they actually work on `Data`
structures (even if `Data` is not a type per se).

I think we should move them to a `Data / Tuple` section in the stdlib.

Also, I'd like to have `merge` and `deepMerge` in the same section.
