## Problem to solve

1. When loading an example, if some input data is provided, then the input
   block should be uncollapsed by default
2. The items filtering example on homepage should have json input data loaded
   when loaded in playground (no need to show the data on home page itself)
