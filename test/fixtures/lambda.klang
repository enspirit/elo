fn( x ~> x )
fn( x ~> x * 2 )
fn( x, y ~> x + y )
fn( a, b, c ~> a + b + c )
fn( x ~> x * x + 1 )
fn( price ~> price * 1.21 )
fn( _ ~> _.budget * 1.21 )
fn( item ~> item.price + item.tax )
fn( x ~> let doubled = x * 2 in doubled + 1 )
fn( x ~> if x > 0 then x else 0 - x )
let double = fn( x ~> x * 2 ) in double(5)
let add = fn( x, y ~> x + y ) in add(3, 4)
let double = fn( x ~> x * 2 ) in assert(double(2) == 4)
